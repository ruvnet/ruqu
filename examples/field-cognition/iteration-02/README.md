# Field cognition iteration 2

This is a bounded synthetic experiment, not evidence of consciousness, integrated information, quantum advantage, or quantum hardware behavior.

Install and run with Node 20 or newer:

```sh
npm ci
npm test
npm run benchmark
```

The development search is reproducible with `npm run develop`, but the holdout protocol and selected configuration must not be changed after inspecting holdout results.
